export const metadata = {
  title: 'Premarket - Property Insights',
  description: 'Property engagement metrics and buyer insights powered by Premarket',
};

export default function RexEmbedLayout({ children }) {
  return <div className="min-h-screen bg-white">{children}</div>;
}
